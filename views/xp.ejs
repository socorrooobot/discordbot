<div class="row">
    <div class="col-12 text-center mb-5">
        <h1 class="display-4 fw-bold text-white"><i class="bi bi-trophy me-2 text-warning"></i> Ranking de Almas</h1>
        <p class="lead text-muted">Os usuários mais ativos e poderosos do ecossistema.</p>
    </div>
</div>

<div class="card mb-5 border-0 shadow-lg bg-dark text-white">
    <div class="card-header bg-transparent border-secondary py-3">
        <h5 class="mb-0"><i class="bi bi-lightning-charge-fill me-2 text-warning"></i> Multiplicador de Experiência</h5>
    </div>
    <div class="card-body p-4">
        <div class="row align-items-center">
            <div class="col-md-6">
                <h6 class="text-uppercase text-muted fw-bold mb-1">Taxa de XP Atual</h6>
                <h2 class="display-5 fw-bold text-warning"><%= xpMultiplier %>x</h2>
            </div>
            <div class="col-md-6">
                <form method="POST" action="/xp/multiplier">
                    <label class="form-label text-muted small fw-bold">Novo Multiplicador de XP</label>
                    <div class="input-group">
                        <input type="number" name="multiplier" step="0.1" value="<%= xpMultiplier %>" class="form-control bg-black border-secondary text-white" required>
                        <button type="submit" class="btn btn-warning px-4 fw-bold">Atualizar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="card border-0 shadow-lg bg-dark text-white">
    <div class="card-header bg-transparent border-secondary py-3">
        <h5 class="mb-0"><i class="bi bi-star-fill me-2 text-warning"></i> Melhores do Rank</h5>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-dark table-hover mb-0 align-middle">
                <thead class="bg-black">
                    <tr>
                        <th class="ps-4 py-3 text-center" style="width: 80px">Rank</th>
                        <th class="py-3">Usuário</th>
                        <th class="py-3 text-center">Nível</th>
                        <th class="py-3 text-center">XP Atual</th>
                        <th class="py-3 pe-4 text-center">XP Acumulado</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach((userItem, index) => { %>
                    <tr class="<%= index === 0 ? 'table-active' : '' %>">
                        <td class="ps-4 text-center">
                            <span class="fs-4">
                                <% if(index === 0) { %>🥇<% } 
                                   else if(index === 1) { %>🥈<% } 
                                   else if(index === 2) { %>🥉<% } 
                                   else { %><strong>#<%= index + 1 %></strong><% } %>
                            </span>
                        </td>
                        <td>
                            <div class="d-flex align-items-center">
                                <img src="<%= userItem.avatar %>" width="40" class="rounded-circle me-3 border border-secondary shadow">
                                <div>
                                    <div class="fw-bold"><%= userItem.username %></div>
                                    <small class="text-muted"><%= userItem.id %></small>
                                </div>
                            </div>
                        </td>
                        <td class="text-center">
                            <span class="badge bg-primary px-3 py-2">Nível <%= userItem.level %></span>
                        </td>
                        <td class="text-center text-info fw-bold">
                            <%= userItem.xp.toLocaleString() %> XP
                        </td>
                        <td class="pe-4 text-center">
                            <div class="small text-muted mb-1">Total</div>
                            <div class="fw-bold text-white"><%= userItem.totalXP.toLocaleString() %> XP</div>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</div>
