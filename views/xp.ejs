
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>XP - Miku Diva Bot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                🎤 Miku Diva Dashboard
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="bi bi-house"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/economy"><i class="bi bi-cash-coin"></i> Economia</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/xp"><i class="bi bi-star"></i> XP</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admins"><i class="bi bi-shield"></i> Admins</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/servers"><i class="bi bi-discord"></i> Servidores</a>
                    </li>
                </ul>
                <div class="d-flex align-items-center">
                    <img src="<%= user.displayAvatarURL() %>" alt="Avatar" class="rounded-circle me-2" width="32" height="32">
                    <span class="text-white me-3"><%= user.username %></span>
                    <a href="/logout" class="btn btn-outline-light btn-sm">Sair</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container-fluid py-4">
        <h1 class="mb-4">⭐ Gerenciar XP</h1>

        <div class="card mb-4">
            <div class="card-body">
                <h5>Multiplicador de XP: <%= xpMultiplier %>x</h5>
                <form method="POST" action="/xp/multiplier" class="d-flex gap-2 align-items-end">
                    <div>
                        <label class="form-label">Novo Multiplicador</label>
                        <input type="number" name="multiplier" step="0.1" value="<%= xpMultiplier %>" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Atualizar</button>
                </form>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-dark table-hover">
                <thead>
                    <tr>
                        <th>Rank</th>
                        <th>Usuário</th>
                        <th>Nível</th>
                        <th>XP Atual</th>
                        <th>XP Total</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach((userItem, index) => { %>
                    <tr>
                        <td><%= index === 0 ? '🥇' : index === 1 ? '🥈' : index === 2 ? '🥉' : (index + 1) %></td>
                        <td>
                            <img src="<%= userItem.avatar %>" width="32" class="rounded-circle me-2">
                            <%= userItem.username %>
                        </td>
                        <td>Nível <%= userItem.level %></td>
                        <td><%= userItem.xp %> XP</td>
                        <td><%= userItem.totalXP %> XP</td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
