<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Miku Diva Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #000;
            background-image: 
                radial-gradient(circle at 20% 30%, rgba(0, 251, 255, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(0, 119, 255, 0.15) 0%, transparent 50%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .login-card {
            background: rgba(15, 15, 15, 0.95);
            border-radius: 20px;
            border: 2px solid #00fbff;
            box-shadow: 0 0 30px rgba(0, 251, 255, 0.2);
            padding: 2.5rem;
            max-width: 450px;
            width: 100%;
            backdrop-filter: blur(10px);
        }
        .miku-logo {
            font-size: 3rem;
            background: linear-gradient(90deg, #00fbff, #0077ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: bold;
            text-align: center;
            margin-bottom: 0.5rem;
        }
        .form-control {
            background: rgba(0,0,0,0.5);
            border: 1px solid #333;
            color: #fff;
            padding: 0.8rem;
        }
        .form-control:focus {
            background: rgba(0,0,0,0.7);
            border-color: #00fbff;
            box-shadow: 0 0 10px rgba(0, 251, 255, 0.3);
            color: #fff;
        }
        .btn-primary {
            background: linear-gradient(90deg, #00fbff, #0077ff);
            border: none;
            padding: 0.8rem;
            font-weight: bold;
            letter-spacing: 1px;
            transition: transform 0.2s;
        }
        .btn-primary:hover {
            transform: scale(1.02);
            box-shadow: 0 0 20px rgba(0, 251, 255, 0.4);
        }
        .register-info {
            background: rgba(0, 251, 255, 0.05);
            border-left: 3px solid #00fbff;
            padding: 10px;
            font-size: 0.85rem;
            margin-top: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="login-card">
        <div class="miku-logo">🎤 MIKU DIVA</div>
        <p class="text-center text-white-50 mb-4">Acesse seu painel de controle</p>
        
        <% if (error) { %>
            <div class="alert alert-danger bg-danger bg-opacity-10 border-danger text-danger border-0 rounded-3 mb-4">
                <i class="bi bi-exclamation-triangle-fill me-2"></i><%= error %>
            </div>
        <% } %>

        <form method="POST" action="/login">
            <div class="mb-3">
                <label for="userId" class="form-label small text-white-50 fw-bold">DISCORD USER ID</label>
                <div class="input-group">
                    <span class="input-group-text bg-black border-secondary text-info"><i class="bi bi-person-fill"></i></span>
                    <input type="text" class="form-control" id="userId" name="userId" required placeholder="Ex: 1461033378701639955">
                </div>
            </div>
            <div class="mb-4">
                <label for="password" class="form-label small text-white-50 fw-bold">SUA SENHA</label>
                <div class="input-group">
                    <span class="input-group-text bg-black border-secondary text-info"><i class="bi bi-lock-fill"></i></span>
                    <input type="password" class="form-control" id="password" name="password" required placeholder="Sua senha secreta">
                </div>
            </div>
            <button type="submit" class="btn btn-primary w-100 mb-3 text-uppercase">Entrar no Palco <i class="bi bi-arrow-right-short"></i></button>
        </form>

        <div class="register-info text-white-50">
            <i class="bi bi-info-circle-fill text-info me-2"></i>Ainda não tem conta? Use o comando <strong>!registrar <sua_senha></strong> no Discord para criar seu acesso agora mesmo!
        </div>
    </div>
</body>
</html>